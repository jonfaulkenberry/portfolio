<div class="row">
  <div class="medium-12 columns">
    <% if @projects.empty? %>
      <h3>There are no projects to show!</h3>
    <% else %>
      <%= render @projects %>
    <% end %>
    <p><%= link_to "New Project", new_project_path, remote: true, id: :new_project_link %></p>
  </div>
</div>
